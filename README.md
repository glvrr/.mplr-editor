# .mplr-editor
MPLR Editor — a browser-based layout editor for macro pads. Open, create, and edit .mplr preset files: configure key bindings, encoder actions, and layers with a visual grid interface. No install, no dependencies — just one HTML file.

Hosted on: https://glvrr.github.io/.mplr-editor/
